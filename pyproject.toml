[project]
name = "titanic mcp"
version = "0.1.0"
description = "Titanic dataset MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "python-dotenv>=1.0.1",
    "fastmcp",
    "openai>=1.101.0",
    "pandas>=2.3.2",
    "requests>=2.32.5",
]
